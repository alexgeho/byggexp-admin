# Production API URL (baked into the client bundle at build time).
# GitHub Actions: set as secret NEXT_PUBLIC_API_URL.
# Local backend example: http://localhost:3000
# Production backend example: https://api.byggexp.se
NEXT_PUBLIC_API_URL=http://localhost:3000
